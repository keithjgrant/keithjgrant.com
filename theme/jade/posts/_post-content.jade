main.l-main
  article.post
    if !isPostPage
      h2.post-title
        a(href=post.url)!= post.title
    .post-content!= post.getContent()
    date.post-date!= post.formattedDate
  footer
    a(href="/posts") See more posts
